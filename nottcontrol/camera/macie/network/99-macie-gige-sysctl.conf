# MACIE / MSAC GigE needs a large SO_RCVBUF (~134217 KB ≈ 128 MiB).
# Kernel doubles SO_RCVBUF for bookkeeping, so rmem_max must be >= 256 MiB.
#
# Install as /etc/sysctl.d/99-zz-macie-gige.conf (must sort AFTER
# 99-sysctl.conf, which is /etc/sysctl.conf). A name like 99-macie-gige.conf
# loses to Lucid/Arena leftovers (rmem_max=33554432) when you run
# `sysctl --system`. Then: sudo sysctl -p /etc/sysctl.d/99-zz-macie-gige.conf
net.core.rmem_max = 268435456
net.core.rmem_default = 134217728
net.core.wmem_max = 268435456
net.core.wmem_default = 134217728
net.core.netdev_max_backlog = 50000
