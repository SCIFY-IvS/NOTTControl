# NetworkManager must not manage the MACIE camera NIC.
# Campus / VNC stays on enp2s0f0. Copy to:
#   /etc/NetworkManager/conf.d/99-unmanaged-macie.conf
[keyfile]
unmanaged-devices=interface-name:enp2s0f1
